# Excel Merger 사용자 가이드

## 목차
1. [프로그램 소개](#1-프로그램-소개)
2. [시작하기](#2-시작하기)
3. [사용 방법](#3-사용-방법)
4. [결과 확인](#4-결과-확인)
5. [로그 파일](#5-로그-파일)
6. [주의사항](#6-주의사항)
7. [FAQ](#7-faq)

---

## 1. 프로그램 소개

### Excel Merger란?
**Excel Merger**는 여러 개의 엑셀 파일을 하나로 병합하는 Windows용 프로그램입니다.

### 주요 기능
- ✅ 1~100개의 엑셀 파일을 하나로 병합
- ✅ 각 파일의 `summary` 시트 2행 데이터 자동 추출
- ✅ 파일 선택부터 병합까지 자동화
- ✅ 한국어 파일명 완벽 지원
- ✅ 설치 불필요 (실행 파일만으로 작동)

### 사용 사례
- 여러 부서의 동일 형식 보고서 통합
- 월별/분기별 데이터 취합
- 다수 파일의 summary 정보 수집

---

## 2. 시작하기

### 시스템 요구사항
- **운영체제**: Windows 10 이상
- **메모리**: 4GB RAM 이상 권장
- **디스크 공간**: 50MB 이상

### 프로그램 실행
1. `ExcelMerger.exe` 파일을 원하는 위치에 저장
2. 파일을 더블 클릭하여 실행
3. 관리자 권한 불필요

**첫 실행 시**:
- Windows Defender가 경고를 표시할 수 있습니다
- "추가 정보" → "실행" 클릭으로 실행
- 자세한 내용은 ANTIVIRUS_CHECK.md 참조

---

## 3. 사용 방법

### 3.1 기본 사용 흐름

```
1. ExcelMerger.exe 실행
   ↓
2. 결과 파일 저장 위치 선택
   ↓
3. 병합할 엑셀 파일 선택 (1~100개)
   ↓
4. 자동 병합 시작
   ↓
5. 결과 파일 생성 완료
```

### 3.2 단계별 상세 설명

#### Step 1: 결과 파일 저장 위치 선택

프로그램을 실행하면 첫 번째로 저장 위치를 묻는 창이 나타납니다.

**작업**:
1. 결과 파일을 저장할 폴더 선택
2. "폴더 선택" 버튼 클릭

**Tips**:
- 접근하기 쉬운 위치 선택 (예: 바탕화면, 문서 폴더)
- 로그 파일도 이 위치에 함께 저장됩니다

#### Step 2: 병합할 파일 선택

파일 선택 방법은 두 가지입니다:

**방법 A: 개별 파일 선택**
1. 파일 선택 대화상자에서 엑셀 파일 선택
2. `Ctrl` 키를 누른 상태에서 여러 파일 선택
3. "열기" 버튼 클릭

**방법 B: 폴더 단위 선택**
1. 폴더 선택 대화상자에서 엑셀 파일이 있는 폴더 선택
2. 폴더 내 모든 `.xlsx`, `.xls` 파일 자동 인식

**제한사항**:
- 최대 100개 파일까지 선택 가능
- 101개 이상 선택 시 오류 메시지 표시

#### Step 3: 자동 병합 진행

파일을 선택하면 자동으로 병합이 시작됩니다.

**진행 과정**:
1. 각 파일의 `summary` 시트 찾기
2. 첫 번째 파일의 1행(헤더) 확인
3. 모든 파일의 2행 데이터 추출
4. 하나의 파일로 병합
5. 결과 파일 저장

**처리 시간**:
- 10개 파일: 약 1초
- 50개 파일: 약 2-3초
- 100개 파일: 5초 이내

#### Step 4: 완료 메시지 확인

병합이 완료되면 성공 메시지가 표시됩니다.

**메시지 예시**:
```
=== 병합 성공 ===

저장 위치: C:\Users\사용자\Documents\merged_summary_20251213_1430.xlsx
병합된 파일 수: 25개

로그 파일: C:\Users\사용자\Documents\merger_log_20251213_1430.txt
```

---

## 4. 결과 확인

### 4.1 결과 파일

**파일명 형식**: `merged_summary_yyyyMMdd_HHmm.xlsx`
- 예: `merged_summary_20251213_1430.xlsx`
- 날짜와 시간이 자동으로 파일명에 포함됩니다

**파일 구조**:
- **시트명**: `summary`
- **1행**: 첫 번째 파일의 헤더
- **2행 이후**: 각 파일의 2행 데이터가 순서대로 추가

**예시**:
```
파일 A의 summary 시트:
1행: 이름, 나이, 부서
2행: 홍길동, 25, 영업팀

파일 B의 summary 시트:
1행: 이름, 나이, 부서
2행: 김철수, 30, 기획팀

결과 파일:
1행: 이름, 나이, 부서
2행: 홍길동, 25, 영업팀
3행: 김철수, 30, 기획팀
```

### 4.2 동일 파일명 처리

같은 시간(분 단위)에 여러 번 실행하면 자동으로 번호가 증가합니다:
- `merged_summary_20251213_1430.xlsx`
- `merged_summary_20251213_1430(1).xlsx`
- `merged_summary_20251213_1430(2).xlsx`

---

## 5. 로그 파일

### 5.1 로그 파일이란?

병합 과정의 모든 정보가 기록되는 텍스트 파일입니다.

**파일명 형식**: `merger_log_yyyyMMdd_HHmm.txt`
- 결과 파일과 같은 위치에 저장됩니다

### 5.2 로그 내용

```
[2025-12-13 14:30:00] [INFO] 병합 시작
[2025-12-13 14:30:01] [SUCCESS] Report_A.xlsx - 5 columns extracted
[2025-12-13 14:30:02] [SKIP] Report_B.xlsx - summary 시트가 없습니다.
[2025-12-13 14:30:03] [SUCCESS] Report_C.xlsx - 5 columns extracted
[2025-12-13 14:30:04] [ERROR] Report_D.xlsx - 파일이 잠겨있습니다...
[2025-12-13 14:30:05] [INFO] === 병합 성공 ===
[2025-12-13 14:30:05] [INFO] 헤더 파일: Report_A.xlsx
[2025-12-13 14:30:05] [INFO] 병합된 파일 수: 2
```

### 5.3 로그 상태 코드

| 코드 | 의미 | 설명 |
|------|------|------|
| **SUCCESS** | 성공 | 파일이 정상적으로 처리됨 |
| **SKIP** | 건너뜀 | 파일이 건너뛰어짐 (이유 포함) |
| **ERROR** | 오류 | 오류 발생 (계속 진행) |
| **INFO** | 정보 | 일반 정보 메시지 |

### 5.4 로그 활용

- **문제 파악**: 어떤 파일이 건너뛰어졌는지 확인
- **오류 추적**: 오류 원인 파악
- **결과 검증**: 예상한 파일 수가 병합되었는지 확인

---

## 6. 주의사항

### 6.1 파일 요구사항

✅ **필수 조건**:
- `.xlsx` 또는 `.xls` 파일
- `summary` 시트 존재 (대소문자 구분 안 함)
- 2행에 데이터 존재

❌ **지원하지 않는 경우**:
- CSV, PDF 등 다른 형식
- summary 시트가 없는 파일
- 2행이 완전히 비어있는 파일

### 6.2 헤더 검증 규칙

첫 번째 성공 파일의 1행(헤더)은 다음 조건을 만족해야 합니다:

❌ **불가능한 헤더**:
- 1행 전체가 비어있음
- 1행에 병합된 셀 포함
- 1행에 날짜 형식 포함
- 1행에 수식 포함

✅ **올바른 헤더**:
- 텍스트 또는 숫자
- 병합되지 않은 개별 셀
- 수식이 아닌 값

**예시**:
```
✅ 올바른 헤더:
이름 | 나이 | 부서 | 직급

❌ 잘못된 헤더:
이름 | 나이 | 2025-01-01 | =SUM(A1:A10)
          (날짜)         (수식)

❌ 잘못된 헤더:
[이름과 나이가 병합된 셀] | 부서
```

### 6.3 파일 처리 규칙

1. **처리 순서**: 선택한 순서대로 처리
2. **헤더 기준**: 첫 번째 성공 파일의 헤더 사용
3. **스킵 처리**: 문제 있는 파일은 건너뛰고 계속 진행
4. **열 개수**: 파일마다 열 개수가 달라도 정상 병합

### 6.4 성능

- **권장**: 파일 개수 50개 이하, 각 파일 1MB 이하
- **최대**: 100개 파일, 5초 이내 처리
- **대용량**: 파일이 크거나 많으면 시간이 더 소요될 수 있음

---

## 7. FAQ

### Q1: summary 시트가 없다는 오류가 나와요
**A**: 각 엑셀 파일에 `summary`라는 이름의 시트가 있는지 확인하세요. 대소문자는 구분하지 않으므로 `Summary`, `SUMMARY`도 가능합니다.

### Q2: 헤더 검증 오류가 발생해요
**A**: 첫 번째 파일의 1행(헤더)에 다음이 포함되어 있는지 확인하세요:
- 병합된 셀
- 날짜 형식
- 수식 (=로 시작하는 셀)

이들을 제거하고 다시 시도하세요.

### Q3: 파일이 잠겨있다는 오류가 나와요
**A**: 다른 프로그램(Excel 등)에서 해당 파일을 열고 있지 않은지 확인하세요. 모든 파일을 닫고 다시 시도하세요.

### Q4: 한국어 파일명을 사용할 수 있나요?
**A**: 네, 완벽하게 지원합니다. 파일명과 데이터 모두 한국어 사용 가능합니다.

### Q5: 열 개수가 파일마다 다른데 괜찮나요?
**A**: 네, 문제없습니다. 각 파일의 열 개수가 달라도 자동으로 처리됩니다.

### Q6: 2행이 비어있으면 어떻게 되나요?
**A**: 해당 파일은 자동으로 건너뛰어지고 로그에 기록됩니다. 다른 파일들은 정상적으로 병합됩니다.

### Q7: 원본 파일이 수정되나요?
**A**: 아니요, 원본 파일은 절대 수정되지 않습니다. 읽기 전용으로만 접근합니다.

### Q8: Excel이 설치되어 있어야 하나요?
**A**: 아니요, Excel이 설치되어 있지 않아도 작동합니다.

### Q9: 100개 이상의 파일을 병합하려면?
**A**: 파일을 두 그룹으로 나누어 각각 병합한 후, 결과 파일 2개를 다시 병합하세요.

### Q10: 바이러스로 감지되는데요?
**A**: 오탐(False Positive)입니다. ANTIVIRUS_CHECK.md를 참조하여 안전성을 확인하세요.

---

## 8. 추가 도움말

### 관련 문서
- **설치 가이드**: INSTALLATION.md
- **문제 해결**: TROUBLESHOOTING.md
- **바이러스 검사**: ANTIVIRUS_CHECK.md
- **빌드 가이드**: BUILD.md (개발자용)

### 지원
- 문제가 해결되지 않으면 TROUBLESHOOTING.md를 먼저 확인하세요
- 소스 코드: https://github.com/[repository]
- 버그 리포트: GitHub Issues

---

## 요약

1. **실행**: ExcelMerger.exe 더블 클릭
2. **저장 위치**: 결과 파일 저장 폴더 선택
3. **파일 선택**: 병합할 엑셀 파일 선택 (1~100개)
4. **자동 병합**: 각 파일의 summary 시트 2행 데이터 병합
5. **결과 확인**: `merged_summary_[날짜시간].xlsx` 파일 생성
6. **로그 확인**: `merger_log_[날짜시간].txt`에서 상세 정보 확인

간단하고 빠르게 엑셀 파일을 병합하세요!
