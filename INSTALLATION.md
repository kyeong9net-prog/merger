# Excel Merger 설치 및 실행 가이드

## 개요
Excel Merger는 설치가 필요 없는 **독립 실행 파일(Standalone Executable)**입니다.
다운로드 후 바로 실행할 수 있습니다.

---

## 1. 시스템 요구사항

### 최소 사양
- **운영체제**: Windows 10 (64-bit) 이상
- **메모리**: 4GB RAM
- **디스크 공간**: 50MB 여유 공간
- **화면 해상도**: 1024×768 이상

### 권장 사양
- **운영체제**: Windows 10/11 (64-bit)
- **메모리**: 8GB RAM 이상
- **디스크 공간**: 100MB 여유 공간
- **화면 해상도**: 1920×1080

### 필요하지 않은 것
- ❌ Python 설치 불필요
- ❌ Microsoft Excel 설치 불필요
- ❌ .NET Framework 설치 불필요
- ❌ 관리자 권한 불필요
- ❌ 인터넷 연결 불필요

---

## 2. 다운로드

### 파일 다운로드
1. 배포 페이지 또는 공유 링크에서 파일 다운로드
2. 다운로드 받을 파일: `ExcelMerger.exe` (약 20-30MB)

### 다운로드 위치 권장
- **바탕화면**: 쉬운 접근
- **문서 폴더**: 정리된 관리
- **전용 폴더**: `C:\Tools\ExcelMerger\`

---

## 3. 첫 실행

### 3.1 실행 방법

#### 방법 1: 더블 클릭
1. `ExcelMerger.exe` 파일 찾기
2. 마우스로 더블 클릭
3. 프로그램 실행

#### 방법 2: 명령 프롬프트
```cmd
cd C:\다운로드\경로
ExcelMerger.exe
```

### 3.2 Windows Defender 경고

첫 실행 시 Windows Defender가 경고를 표시할 수 있습니다:

```
Windows의 PC 보호
알 수 없는 게시자의 앱이 장치에 변경을 허용하시겠습니까?
```

**대응 방법**:

#### 옵션 A: 추가 정보 → 실행
1. "추가 정보" 클릭
2. "실행" 버튼이 나타남
3. "실행" 클릭

#### 옵션 B: Windows Defender 예외 추가
1. Windows 설정 (`Win + I`)
2. "업데이트 및 보안" → "Windows 보안"
3. "바이러스 및 위협 방지"
4. "설정 관리"
5. "제외 추가" → "파일"
6. `ExcelMerger.exe` 선택

자세한 내용은 `ANTIVIRUS_CHECK.md` 참조

### 3.3 첫 실행 화면

프로그램이 실행되면:
1. 결과 파일 저장 위치 선택 대화상자 표시
2. 폴더 선택
3. 파일 선택 대화상자 표시

---

## 4. 바로가기 만들기 (선택사항)

### 바탕화면 바로가기
1. `ExcelMerger.exe` 우클릭
2. "바로 가기 만들기" 선택
3. 바로가기를 바탕화면으로 이동

### 시작 메뉴에 고정
1. `ExcelMerger.exe` 우클릭
2. "시작 메뉴에 고정" 선택

---

## 5. 제거 방법

### 프로그램 삭제
1. `ExcelMerger.exe` 파일 삭제
2. 끝!

### 관련 파일 정리 (선택사항)
프로그램이 생성한 파일:
- 결과 파일: `merged_summary_*.xlsx`
- 로그 파일: `merger_log_*.txt`

필요 없으면 함께 삭제하세요.

### 레지스트리 정리
- Excel Merger는 레지스트리를 사용하지 않습니다
- 별도 정리 불필요

---

## 6. 업데이트

### 새 버전 설치
1. 새로운 `ExcelMerger.exe` 다운로드
2. 기존 파일 덮어쓰기
3. 끝!

### 설정 유지
- 별도 설정 파일 없음
- 업데이트 시 설정 유지 불필요

---

## 7. 다중 사용자 환경

### 네트워크 드라이브
- 네트워크 드라이브에 저장 가능
- 여러 사용자가 동시에 실행 가능
- 각자의 결과 파일 생성

### 주의사항
- 동시에 같은 엑셀 파일 선택 시 충돌 가능
- 결과 파일은 각 사용자의 선택한 위치에 저장

---

## 8. 문제 해결

### 프로그램이 실행되지 않아요
1. Windows 버전 확인 (Windows 10 이상)
2. 관리자 권한으로 실행 시도
3. 바이러스 백신 예외 추가

### 실행 시 오류가 발생해요
1. 다시 다운로드
2. 파일 무결성 확인
3. TROUBLESHOOTING.md 참조

### 느리게 실행돼요
- 첫 실행 시 압축 해제로 약간 느릴 수 있음
- 두 번째 실행부터는 빠름
- 백신 실시간 검사가 영향을 줄 수 있음

---

## 9. 기술 정보

### 실행 파일 정보
- **파일명**: ExcelMerger.exe
- **크기**: 약 20-30MB
- **형식**: PE32+ executable (Windows)
- **아키텍처**: x86-64

### 포함된 구성 요소
- Python 3.9+ 인터프리터
- openpyxl 라이브러리
- 필요한 모든 의존성

---

## 10. FAQ

**Q: 설치 프로그램이 있나요?**
A: 아니요, 설치 과정이 없습니다. .exe 파일만 다운로드하면 됩니다.

**Q: 레지스트리에 항목을 추가하나요?**
A: 아니요, 레지스트리를 전혀 사용하지 않습니다.

**Q: 자동 업데이트 기능이 있나요?**
A: 아니요, 수동으로 새 버전을 다운로드해야 합니다.

**Q: 여러 버전을 동시에 사용할 수 있나요?**
A: 네, 서로 다른 폴더에 저장하면 여러 버전을 유지할 수 있습니다.

**Q: macOS나 Linux에서 사용할 수 있나요?**
A: 아니요, Windows 전용입니다. 소스 코드로 Python 환경에서는 실행 가능합니다.

---

## 요약

1. **다운로드**: ExcelMerger.exe 파일 받기
2. **저장**: 원하는 위치에 저장
3. **실행**: 더블 클릭으로 즉시 실행
4. **사용**: USER_GUIDE.md 참조
5. **삭제**: 파일 삭제로 간단히 제거

설치가 필요 없어 누구나 쉽게 사용할 수 있습니다!

---

**다음 단계**: USER_GUIDE.md를 읽고 프로그램 사용 방법을 확인하세요.
