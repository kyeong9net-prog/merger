[pytest]
# pytest 설정 파일 (Phase 5)

# 테스트 디렉토리
testpaths = tests

# 테스트 파일 패턴
python_files = test_*.py

# 테스트 클래스 패턴
python_classes = Test*

# 테스트 함수 패턴
python_functions = test_*

# 상세 출력
addopts = -v --tb=short

# 경고 무시
filterwarnings =
    ignore::DeprecationWarning
