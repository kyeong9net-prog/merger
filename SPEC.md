# SPEC: 엑셀 파일 병합 도구

## 개요

여러 개의 엑셀 파일(1~100개)을 하나로 통합하는 Windows용 .exe 실행 파일
- 각 파일의 `summary` 시트 2행 데이터를 추출하여 하나의 결과 파일로 병합
- 사용자 입력 최소화 및 자동화된 병합 프로세스 제공

---

## Phase 1: 기본 기능 구현

### 파일 선택 기능
- [x] 1개~100개의 엑셀 파일 선택 UI 구현
- [x] 다중 파일 선택 지원
- [x] 폴더 단위 선택 지원 (폴더 내 엑셀 파일만 자동 인식)
- [x] 최대 100개 파일 제한 검증 및 오류 메시지 표시

### 엑셀 데이터 추출
- [x] summary 시트 검색 (대소문자 구분 없이)
- [x] 2행 전체 데이터 추출 (A2부터 마지막 컬럼까지)
- [x] 2행 전체가 비어있는 경우 스킵 처리
- [x] 스킵된 파일 로그 기록

### 헤더 처리
- [x] 첫 번째 성공 파일의 1행을 헤더로 사용
- [x] 1행 전체가 비어있는 경우 오류 메시지 및 병합 중단
- [x] 1행에 병합 셀이 있는 경우 오류 메시지 및 병합 중단
- [x] 1행에 날짜 형식이 포함된 경우 오류 메시지 및 병합 중단
- [x] 1행에 수식이 포함된 경우 오류 메시지 및 병합 중단

### 결과 파일 생성
- [x] 모든 2행 데이터를 순차적으로 병합
- [x] 파일명 형식: `merged_summary_yyyyMMdd_HHmm.xlsx`
- [x] 사용자 지정 경로에 저장
- [x] 동일 파일명 존재 시 자동 번호 증가 (결과(1).xlsx, 결과(2).xlsx)

---

## Phase 2: 예외 처리 및 로그

### 파일 포맷 검증
- [x] .xlsx, .xls 확장자만 허용
- [x] 다른 확장자는 스킵 및 로그 기록

### 시트 검증
- [x] summary 시트가 없는 파일 스킵 및 로그 기록
- [x] summary 시트 2행 전체가 비어있는 파일 스킵 및 로그 기록

### 파일 접근 오류 처리
- [x] 파일 잠김(File Locked) 상태 처리
- [x] 잠긴 파일은 스킵하고 전체 작업 계속 진행
- [x] 오류 메시지 출력

### 로그 출력 시스템
- [x] 시간 스탬프 포함
- [x] 처리 상태 표시 (SUCCESS, SKIP, ERROR)
- [x] 파일명 기록
- [x] 상세 이유 기록
- [x] 예시: `[2025-12-10 23:55:00] [SUCCESS] Report_A.xlsx - 15 columns merged.`
- [x] 로그 파일 저장 기능

---

## Phase 3: 기술 구현

### 실행 환경
- [ ] Windows 10 이상 지원
- [ ] 단독 실행 .exe 파일 생성
- [ ] 별도 설치 불필요

### 개발 스택 선정
- [ ] Python + openpyxl + PyInstaller 또는
- [ ] C# (.NET 6 이상) + ClosedXML
- [ ] 개발 언어 최종 결정

### 성능 최적화
- [ ] 100개 파일 병합 5초 이내 처리
- [ ] 메모리 사용량 안정성 확보

---

## Phase 4: 품질 요구사항

### 다국어 및 호환성
- [ ] 파일명에 한국어 포함 가능
- [ ] summary 시트의 열 개수가 파일마다 달라도 정상 병합

### 데이터 안전성
- [ ] 원본 파일 읽기 전용(read-only) 처리
- [ ] 원본 파일 수정 금지
- [ ] 결과 파일 생성 실패 시 명확한 오류 메시지 제공

### 오류 메시지 표준화
- [ ] 표준화된 포맷으로 오류 메시지 출력
- [ ] 사용자가 이해하기 쉬운 메시지 작성

---

## Phase 5: 테스트

### 단위 테스트
- [ ] 파일 선택 기능 테스트
- [ ] 데이터 추출 기능 테스트
- [ ] 헤더 검증 로직 테스트
- [ ] 예외 처리 테스트

### 통합 테스트
- [ ] 파일 20개 기준 병합 테스트
- [ ] 파일 50개 기준 병합 테스트
- [ ] 파일 100개 기준 병합 테스트
- [ ] 성능 테스트 (5초 이내 처리)

### 엣지 케이스 테스트
- [ ] 빈 파일 처리
- [ ] summary 시트가 없는 파일 처리
- [ ] 잠긴 파일 처리
- [ ] 열 개수가 다른 파일들 병합
- [ ] 한국어 파일명 처리

---

## Phase 6: 배포

### 패키징
- [ ] .exe 파일 생성
- [ ] exe 파일 크기 최적화
- [ ] 바이러스 검사 통과 확인

### 문서화
- [ ] 사용자 가이드 작성
- [ ] 설치 가이드 작성 (설치 불필요하지만 실행 방법 안내)
- [ ] 트러블슈팅 가이드 작성

### 최종 배포
- [ ] 최종 exe 파일 제공
- [ ] 배포 완료

---

## 향후 확장 가능성 (Optional)

- [ ] summary 시트 외 다른 시트 선택 가능하도록 옵션 추가
- [ ] 2행뿐 아니라 특정 셀 범위 지정 기능
- [ ] CSV/PDF 등 다른 포맷 지원
- [ ] 헤더 자동 통합/정렬 기능

---

## 추가 정책 및 규칙

### 파일 처리 규칙
- [ ] 파일 처리 순서는 선택된 순서대로 처리
- [ ] summary 시트명은 고정 (대소문자 구분 없음)
- [ ] A2 셀이 비어있어도 그대로 수합 (전체 행이 비어있을 때만 스킵)

### 로그 및 모니터링
- [ ] 처리 과정을 로그 파일로 저장
- [ ] 로그 파일명: `merger_log_yyyyMMdd_HHmm.txt`

---

## 일정 및 Milestone

| 단계 | 설명 | 예상 기간 | 상태 |
|------|------|----------|------|
| 요구사항 확정 | PRD 기반으로 기능 정의 | 1일 | - |
| UI 설계 | 화면 구성도 제작 | 1일 | - |
| 개발 | 기능 구현 및 exe 패키징 | 3~4일 | - |
| 테스트 | 파일 20~100개 기준 테스트 | 1일 | - |
| 배포 | 최종 exe 제공 | 0.5일 | - |
