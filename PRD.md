# 프로그램 PRD: 엑셀 파일 병합 도구 (.exe)

## 1. 개요

이 프로그램은 사용자가 지정한 여러 개의 엑셀 파일(1개 이상, 최대 100개)을 하나의 엑셀 파일로 통합하는 기능을 제공한다. 통합 대상은 각 엑셀 파일에 존재하는 `summary` 시트이며, 그 중 **2행(row 2)** 의 데이터를 추출하여 하나의 결과 파일에 순차적으로 쌓아가는 방식이다. 프로그램은 사용자가 쉽게 실행할 수 있도록 **Windows용 .exe 실행 파일** 형태로 제공된다.

---

## 2. 목적

* 여러 실무 부서에서 취합된 동일한 양식의 엑셀 파일을 하나로 통합하는 반복 작업을 자동화
* 사용자 입력 최소화로 편리한 사용 경험 제공
* 변동 없는 고정 구조(summary 시트, 2행 데이터)를 기준으로 정확하고 일관된 데이터 취합

---

## 3. 주요 기능

### 3.1 파일 선택 기능

* 사용자가 1개~100개의 엑셀 파일을 선택할 수 있는 파일 선택 UI 제공
* 다중 파일 선택 및 폴더 단위 선택 지원 (선택된 폴더에 포함된 엑셀 파일만 자동 인식)

### 3.2 엑셀 데이터 추출

각 파일에서 summary라는 이름의 시트를 대소문자 구분 없이 찾는다.
summary 시트의 2행 전체 데이터를 추출한다 (A2부터 마지막 컬럼까지).
2행 데이터 존재 여부 판단 기준:

* **2행 전체가 비어 있는 경우에만** 스킵 처리한다. (A2 하나만 비어 있어도 스킵하지 않음)
* 단, 2행의 모든 셀이 비어있을 때만 "데이터 없음"으로 간주한다.
  스킵된 파일은 로그에 기록한다.

3.3 결과 파일 생성
모든 입력 파일의 2행 데이터를 순차적으로 아래로 추가한 하나의 엑셀 파일 생성

결과 파일의 구성 예:
1행: 헤더 (MVP 필수 정책) — 성공적으로 처리된 **첫 번째 파일의 summary 시트 1행 전체**를 헤더로 사용한다.

헤더 유효성 규칙:

* 1행 전체가 비어 있으면 오류 메시지를 띄우고 전체 병합 중단
* 1행에 병합 셀이 존재하면 오류 메시지 출력 후 병합 중단
* 1행의 어떤 셀이라도 날짜 형식 또는 수식이 포함되어 있으면 오류 메시지 출력 후 병합 중단

2행~: 파일별 병합된 데이터 행
파일명: merged_summary_yyyyMMdd_HHmm.xlsx

3.4 예외 처리 및 로그 출력
다음과 같은 오류 발생 시 해당 파일은 스킵하고 로그에 기록한다.

* summary 시트가 없는 파일 → 스킵 + 로그
* summary 시트 2행 전체가 비어있는 파일 → 스킵 + 로그
* 파일 포맷 오류(.xlsx, .xls 이외 확장자) → 스킵 + 로그
* 최대 100개 파일 제한 초과 시 명확한 오류 메시지 표시

파일 잠김(File Locked) 상태 처리 규칙:

* 파일이 열려 있어 접근 불가한 경우: 오류 메시지 출력 후 **해당 파일만 스킵**, 전체 작업은 계속 진행한다.

로그 출력 형식 강화:
모든 처리 결과(성공, 스킵, 오류)는 시간 스탬프, 상세 처리 상태, 파일명, 상세 이유 포함.
예시: [2025-12-10 23:55:00] [SUCCESS] Report_A.xlsx - 15 columns merged.
예시: [2025-12-10 23:56:10] [SKIP] Report_B.xlsx - summary 시트 2행 전체가 비어있음.

5. 기술 요구사항

### 5.1 실행 환경

* Windows 10 이상
* 별도 설치 없이 단독 실행(.exe)

### 5.2 개발 언어 제안

* Python + openpyxl + PyInstaller

  * 장점: 개발 속도 빠름
  * 단점: exe 용량 증가 가능

또는

* C# (.NET 6 이상) + ClosedXML

  * 장점: exe 용량 작고 안정적
  * 단점: Python보다 개발 시간 길 수 있음

### 5.3 성능 조건

* 100개 파일 병합 기준 5초 이내 처리 목표
* 메모리 사용량 안정적 유지

---

## 6. 품질 요구사항

* 파일명에 한국어 포함 가능
* summary 시트의 열 개수가 파일마다 달라도 정상 병합
* 파일 손상 방지: 원본 파일 수정 금지 (read-only)
* 결과 파일 생성 실패 시 사용자에게 명확한 오류 메시지 제공

---

## 7. 향후 확장 가능성

* summary 시트 외 다른 시트 선택 가능하도록 옵션 추가
* 2행뿐 아니라 특정 셀 범위 지정 기능
* CSV/PDF 등 다른 포맷 지원
* 헤더 자동 통합/정렬 기능

---

## 8. 일정 및 Milestone

| 단계      | 설명                | 예상 기간 |
| ------- | ----------------- | ----- |
| 요구사항 확정 | PRD 기반으로 기능 정의    | 1일    |
| UI 설계   | 화면 구성도 제작         | 1일    |
| 개발      | 기능 구현 및 exe 패키징   | 3~4일  |
| 테스트     | 파일 20~100개 기준 테스트 | 1일    |
| 배포      | 최종 exe 제공         | 0.5일  |

---

## 9. 기타 고려 사항

* summary 시트 이름 대소문자 구분 없음('Summary', 'SUMMARY' 등도 인식 가능하도록 옵션)
* 프로그램의 첫 출시 버전에서는 최소 기능만 포함하는 MVP 방식 권장

2. 파일 처리 순서 규칙은 별도 처리 기준 없이 그대로 사용한다.

3. summary 시트명은 고정이며, 이름이 정확히 일치하면 처리한다.

4. A2 셀이 비어 있어도 그대로 수합하며, 공백 여부로 판단하지 않는다.

5. 처리 대상 파일 확장자는 .xlsx, .xls 만 허용하며 다른 확장자는 무시한다.

6. 첫 번째 성공적으로 읽힌 파일의 1행을 헤더로 사용한다. 단, 첫 번째 행이 비어있거나 병합 셀 포함 또는 날짜/수식이 포함되어 있으면 오류 메시지를 표시하고 병합 작업을 중단한다.

7. 프로그램 실행 시 처리 과정 및 오류를 로그 파일로 저장한다.

8. 결과 파일 기본 저장 위치는 사용자 지정 경로이며, 동일한 파일명이 존재할 경우 "결과(1).xlsx", "결과(2).xlsx" 와 같이 숫자를 증가시켜 저장한다.

9. 오류 메시지는 표준화된 포맷으로 출력한다.

10. 파일이 잠겨 있어 읽기 불가한 경우 오류 메시지를 출력하고 해당 파일은 건너뛴다.
