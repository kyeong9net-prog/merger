# Excel Merger UI 개선 계획

## 현재 상태 분석

### 현재 UI
- ✅ 간단하고 사용하기 쉬움
- ✅ 빠른 작업 가능
- ❌ 시각적으로 단순함
- ❌ 진행 상황을 알 수 없음
- ❌ 선택한 파일 목록을 미리 볼 수 없음
- ❌ 로그를 실시간으로 볼 수 없음

### 현재 흐름
```
1. [대화상자] 저장 위치 선택
2. [대화상자] 파일 선택 방법 (폴더/파일)
3. [대화상자] 파일 선택
4. [자동 병합] (진행 상황 안 보임)
5. [메시지박스] 완료 알림
```

---

## UI 개선 방향

### 방향 1: 심플 UI (권장)
- 한 화면에 모든 기능
- 단계별 버튼
- 진행 상황 표시

### 방향 2: 마법사 UI
- 단계별 화면 전환
- 다음/이전 버튼
- 각 단계마다 설명

### 방향 3: 대시보드 UI
- 좌측: 메뉴
- 중앙: 작업 영역
- 우측: 로그/상태

---

## 추천: 심플 UI 설계

### 메인 윈도우 구성

```
┌─────────────────────────────────────────────────────┐
│  Excel Merger                              [ _ □ X ]│
├─────────────────────────────────────────────────────┤
│                                                      │
│  📁 1. 저장 위치 선택                                │
│  ┌────────────────────────────────────────────┐    │
│  │ C:\Users\...\결과파일                       │    │
│  └────────────────────────────────────────────┘    │
│  [찾아보기]                                         │
│                                                      │
│  📂 2. 병합할 파일 선택                              │
│  ┌────────────────────────────────────────────┐    │
│  │ ☑ file1.xlsx                                │    │
│  │ ☑ file2.xlsx                                │    │
│  │ ☑ file3.xlsx                                │    │
│  │ ...                               (총 5개)  │    │
│  └────────────────────────────────────────────┘    │
│  [폴더 선택]  [파일 선택]  [모두 제거]              │
│                                                      │
│  ⚙️ 3. 옵션                                          │
│  ☑ 헤더 검증 활성화                                  │
│  ☑ 빈 행 자동 제거                                   │
│                                                      │
│  📊 진행 상황                                        │
│  ████████████████░░░░░░░░ 67% (10/15 files)        │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ [로그]                                       │    │
│  │ ✓ file1.xlsx - 100 rows                     │    │
│  │ ✓ file2.xlsx - 200 rows                     │    │
│  │ ⏳ file3.xlsx - processing...               │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│                 [병합 시작]  [취소]                  │
└─────────────────────────────────────────────────────┘
```

---

## 구현 계획

### Phase 1: 메인 윈도우 생성
**목표**: 기본 윈도우 구조 만들기

**작업**:
1. `ExcelMergerApp` 클래스에서 `self.root.withdraw()` 제거
2. 메인 윈도우 크기 설정 (예: 600x700)
3. 제목 설정 "Excel Merger"
4. 아이콘 설정 (선택사항)

**예상 시간**: 30분

---

### Phase 2: 저장 위치 선택 UI
**목표**: 저장 위치를 GUI에서 표시

**컴포넌트**:
- `tkinter.Label`: "1. 저장 위치 선택"
- `tkinter.Entry`: 선택된 경로 표시 (읽기 전용)
- `tkinter.Button`: "찾아보기" 버튼

**동작**:
- "찾아보기" 클릭 → `filedialog.askdirectory()` 호출
- 선택된 경로를 Entry에 표시

**예상 시간**: 30분

---

### Phase 3: 파일 목록 UI
**목표**: 선택한 파일 목록을 표시하고 관리

**컴포넌트**:
- `tkinter.Label`: "2. 병합할 파일 선택"
- `tkinter.Listbox` 또는 `tkinter.ttk.Treeview`: 파일 목록 표시
- `tkinter.Scrollbar`: 스크롤 기능
- 버튼들:
  - "폴더 선택"
  - "파일 선택"
  - "선택 제거"
  - "모두 제거"

**동작**:
- "폴더 선택" → 폴더 내 모든 .xlsx 파일 추가
- "파일 선택" → 개별 파일 선택
- 파일 클릭 → 체크박스 토글
- "선택 제거" → 체크된 파일만 제거
- "모두 제거" → 전체 목록 비우기

**예상 시간**: 1-2시간

---

### Phase 4: 진행 상황 표시
**목표**: 병합 진행 상황을 실시간으로 표시

**컴포넌트**:
- `tkinter.Label`: "진행 상황"
- `tkinter.ttk.Progressbar`: 진행률 표시바
- `tkinter.Label`: 퍼센트와 파일 수 표시

**동작**:
- 병합 시작 → 진행률 0%
- 파일 처리마다 → 진행률 업데이트
- 완료 → 100%

**주의사항**:
- 병합 작업은 별도 스레드에서 실행
- `threading` 모듈 사용
- UI 업데이트는 메인 스레드에서 (`root.after()` 또는 `queue` 사용)

**예상 시간**: 1-2시간

---

### Phase 5: 실시간 로그 표시
**목표**: 처리 과정을 실시간으로 표시

**컴포넌트**:
- `tkinter.Label`: "[로그]"
- `tkinter.Text`: 로그 메시지 표시 (읽기 전용)
- `tkinter.Scrollbar`: 스크롤 기능

**동작**:
- 파일 처리 시작 → "⏳ file.xlsx - processing..."
- 처리 완료 → "✓ file.xlsx - 100 rows"
- 오류 발생 → "❌ file.xlsx - error message"

**구현**:
- `Logger` 클래스 수정: GUI에 로그 전송
- 또는 새로운 `GUILogger` 클래스 생성

**예상 시간**: 1시간

---

### Phase 6: 옵션 설정 UI (선택사항)
**목표**: 사용자가 설정을 변경할 수 있게

**컴포넌트**:
- `tkinter.Label`: "옵션"
- `tkinter.Checkbutton`:
  - "헤더 검증 활성화"
  - "빈 행 자동 제거"
  - "중복 데이터 제거"

**동작**:
- 체크박스 상태를 변수에 저장
- 병합 시 옵션 적용

**예상 시간**: 30분

---

### Phase 7: 버튼 및 이벤트 처리
**목표**: 모든 버튼 동작 연결

**버튼**:
- "병합 시작": 병합 작업 시작
- "취소": 진행 중인 작업 중단

**동작**:
- "병합 시작" 클릭:
  1. 저장 위치 확인
  2. 파일 목록 확인
  3. 유효성 검사
  4. 병합 시작 (별도 스레드)
  5. 버튼 비활성화
  6. 진행률 표시
  7. 완료 후 메시지 표시
  8. 버튼 다시 활성화

- "취소" 클릭:
  1. 작업 중단 플래그 설정
  2. 스레드 종료 대기
  3. 진행률 초기화

**예상 시간**: 1-2시간

---

## 기술 스택

### 필수
- `tkinter`: 기본 GUI 프레임워크 (이미 사용 중)
- `tkinter.ttk`: 모던한 위젯 (Progressbar, Treeview 등)
- `threading`: 백그라운드 작업 처리

### 선택사항
- `ttkbootstrap`: tkinter 테마 (더 예쁜 UI)
- `customtkinter`: 모던한 UI 라이브러리
- `Pillow`: 아이콘/이미지 처리

---

## 파일 구조 변경

### 현재 구조
```
src/
├── main.py              # 메인 애플리케이션
├── file_selector.py
├── excel_reader.py
├── excel_merger.py
├── file_writer.py
├── header_validator.py
└── logger.py
```

### 제안하는 구조
```
src/
├── main.py              # 진입점
├── gui/
│   ├── __init__.py
│   ├── main_window.py   # 메인 윈도우
│   ├── file_panel.py    # 파일 선택 패널
│   ├── progress_panel.py # 진행률 패널
│   ├── log_panel.py     # 로그 패널
│   └── worker.py        # 백그라운드 작업 스레드
├── core/
│   ├── __init__.py
│   ├── file_selector.py
│   ├── excel_reader.py
│   ├── excel_merger.py
│   ├── file_writer.py
│   ├── header_validator.py
│   └── logger.py
└── utils/
    ├── __init__.py
    └── gui_logger.py    # GUI용 로거
```

---

## 단계별 마일스톤

### 마일스톤 1: 기본 UI (2-3시간)
- [x] Phase 1: 메인 윈도우
- [x] Phase 2: 저장 위치 UI
- [x] Phase 3: 파일 목록 UI

**결과**: 기본적인 UI가 표시되고, 파일 선택이 가능

---

### 마일스톤 2: 기능 연결 (2-3시간)
- [x] Phase 4: 진행 상황 표시
- [x] Phase 5: 로그 표시
- [x] Phase 7: 버튼 이벤트

**결과**: 실제로 병합이 작동하고, 진행 상황을 볼 수 있음

---

### 마일스톤 3: 완성 (1시간)
- [x] Phase 6: 옵션 설정
- [x] 디자인 개선
- [x] 테스트 및 버그 수정

**결과**: 완성된 GUI 애플리케이션

---

## 디자인 개선 옵션

### 옵션 1: 기본 tkinter (현재)
**장점**:
- 추가 설치 불필요
- 가볍고 빠름
- 안정적

**단점**:
- 구식 디자인
- 제한적인 커스터마이징

---

### 옵션 2: ttkbootstrap 사용
**장점**:
- 모던한 테마 (Bootstrap 스타일)
- tkinter와 호환
- 간단한 사용법

**단점**:
- 추가 패키지 설치 필요 (`pip install ttkbootstrap`)

**예시 테마**: superhero, darkly, cosmo, flatly, journal, litera, lumen, minty, pulse, sandstone, united, yeti

---

### 옵션 3: customtkinter 사용
**장점**:
- 매우 모던한 UI
- 다크모드 지원
- 애니메이션 효과

**단점**:
- 더 많은 학습 시간 필요
- 추가 패키지 설치 필요 (`pip install customtkinter`)

---

## 추천 로드맵

### 최소 버전 (4-6시간)
```
1. 메인 윈도우 + 저장 위치 UI
2. 파일 목록 UI (Listbox)
3. 병합 버튼 연결
4. 진행률 표시바
5. 완료 메시지
```

**결과**: 기본적인 GUI가 작동하는 버전

---

### 표준 버전 (8-10시간)
```
최소 버전 +
6. 실시간 로그 표시
7. 파일 추가/제거 버튼
8. 취소 기능
9. 에러 처리 개선
```

**결과**: 완성도 있는 GUI 애플리케이션

---

### 완전 버전 (12-15시간)
```
표준 버전 +
10. 옵션 설정 UI
11. ttkbootstrap 테마 적용
12. 파일 드래그 앤 드롭
13. 단축키 지원
14. 설정 저장/불러오기
```

**결과**: 전문적인 상용 소프트웨어 수준

---

## 주의사항

### 멀티스레딩
- **반드시** 병합 작업을 별도 스레드에서 실행
- tkinter는 스레드 안전하지 않음
- UI 업데이트는 메인 스레드에서만

**해결책**:
```python
# 방법 1: root.after() 사용
root.after(100, update_ui)

# 방법 2: queue 사용
from queue import Queue
log_queue = Queue()
```

---

### PyInstaller 호환성
- GUI 추가 시 새로운 의존성 확인
- `hiddenimports`에 추가 필요:
  - `ttkbootstrap` 사용 시
  - `customtkinter` 사용 시
  - `PIL/Pillow` 사용 시

---

### 테스트
- Windows 10, 11에서 테스트
- 다양한 화면 해상도 테스트
- 많은 파일(100개) 테스트
- 긴 파일명 테스트
- 한글 경로 테스트

---

## 예상 결과

### Before (현재)
```
[파일 선택] → [자동 처리] → [완료]
```
- 간단하지만 진행 상황을 모름
- 프로페셔널하지 않음

### After (UI 개선 후)
```
[메인 화면에서 모든 것을 제어]
- 파일 목록 확인
- 진행 상황 실시간 확인
- 로그 확인
- 언제든 취소 가능
```
- 프로페셔널한 느낌
- 사용자 경험 향상
- 신뢰감 증가

---

## 다음 단계

### 1단계: 결정
- 어떤 버전으로 만들 것인가? (최소/표준/완전)
- 테마를 적용할 것인가? (기본/ttkbootstrap/customtkinter)

### 2단계: 계획 승인
- 이 계획이 괜찮은가?
- 수정할 부분이 있는가?

### 3단계: 구현 시작
- Phase별로 단계적 구현
- 각 Phase마다 테스트
- 커밋 및 푸시

---

## 참고 자료

### tkinter 학습
- 공식 문서: https://docs.python.org/3/library/tkinter.html
- Real Python 튜토리얼: https://realpython.com/python-gui-tkinter/

### ttkbootstrap
- 공식 문서: https://ttkbootstrap.readthedocs.io/
- GitHub: https://github.com/israel-dryer/ttkbootstrap

### customtkinter
- 공식 문서: https://customtkinter.tomschimansky.com/
- GitHub: https://github.com/TomSchimansky/CustomTkinter

---

## 총평

### 장점
- ✅ 사용자 경험 크게 향상
- ✅ 프로페셔널한 느낌
- ✅ 진행 상황 파악 가능
- ✅ 에러 디버깅 쉬움

### 단점
- ❌ 개발 시간 증가 (4-15시간)
- ❌ 코드 복잡도 증가
- ❌ 테스트 난이도 증가

### 추천
**"표준 버전"을 목표로, "최소 버전"부터 단계적으로 구현**

이렇게 하면:
1. 빠르게 기본 UI 완성
2. 점진적으로 기능 추가
3. 중간에 문제 발생 시 롤백 가능

---

**이 계획에 대해 어떻게 생각하시나요?** 🤔

수정하거나 추가하고 싶은 부분이 있으신가요?
