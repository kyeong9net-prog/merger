# Excel Merger 문제 해결 가이드

## 목차
1. [일반적인 문제](#1-일반적인-문제)
2. [파일 관련 오류](#2-파일-관련-오류)
3. [헤더 검증 오류](#3-헤더-검증-오류)
4. [실행 오류](#4-실행-오류)
5. [성능 문제](#5-성능-문제)
6. [바이러스 백신 문제](#6-바이러스-백신-문제)

---

## 1. 일반적인 문제

### 문제: 프로그램이 실행되지 않음

**증상**:
- 더블 클릭해도 아무 반응 없음
- 실행 후 즉시 종료됨

**해결 방법**:

#### 1.1 Windows 버전 확인
```
Win + R → winver
```
Windows 10 이상인지 확인

#### 1.2 관리자 권한으로 실행
1. `ExcelMerger.exe` 우클릭
2. "관리자 권한으로 실행" 선택

#### 1.3 파일 재다운로드
- 다운로드 중 파일이 손상되었을 수 있음
- 파일 크기 확인 (약 20-30MB)

#### 1.4 바이러스 백신 확인
- 백신이 실행을 차단했을 수 있음
- 예외 추가 후 재시도

---

## 2. 파일 관련 오류

### 오류 2.1: "summary 시트가 없습니다"

**증상**:
```
[SKIP] 파일명.xlsx - summary 시트가 없습니다.
```

**원인**:
- 엑셀 파일에 `summary`라는 이름의 시트가 없음

**해결 방법**:
1. 엑셀 파일 열기
2. 시트 탭 확인
3. 시트명을 `summary`로 변경 (대소문자 구분 안 함)

**예시**:
```
✅ 가능: summary, Summary, SUMMARY, SuMmArY
❌ 불가능: sumary, summery, 요약
```

---

### 오류 2.2: "2행 전체가 비어있습니다"

**증상**:
```
[SKIP] 파일명.xlsx - summary 시트 2행 전체가 비어있습니다.
```

**원인**:
- summary 시트의 2행에 데이터가 없음

**해결 방법**:
1. 엑셀 파일 열기
2. summary 시트의 2행 확인
3. 데이터 입력

**주의**:
- 공백이나 빈 문자열도 "비어있음"으로 간주
- 최소 한 셀에 의미 있는 데이터 필요

---

### 오류 2.3: "파일이 잠겨있습니다"

**증상**:
```
[ERROR] 파일명.xlsx - 파일이 잠겨있습니다. 다른 프로그램에서 파일을 닫아주세요.
```

**원인**:
- Excel 등 다른 프로그램에서 파일을 열고 있음
- 네트워크 드라이브에서 다른 사용자가 사용 중

**해결 방법**:
1. Excel 등 파일을 열고 있는 프로그램 종료
2. 작업 관리자에서 Excel 프로세스 강제 종료
3. 파일 복사본으로 시도

**확인 방법**:
```cmd
# PowerShell에서 파일 잠금 확인
Get-Process | Where-Object {$_.Modules.FileName -like "*파일명.xlsx*"}
```

---

### 오류 2.4: "파일 개수 초과"

**증상**:
```
파일 개수 초과

원인: 선택한 파일이 최대 허용 개수(100개)를 초과했습니다.

해결 방법: 파일을 100개 이하로 선택해주세요.
```

**해결 방법**:
1. 파일을 두 그룹으로 나누기
2. 각 그룹 별도 병합
3. 결과 파일 2개를 다시 병합

---

## 3. 헤더 검증 오류

### 오류 3.1: "1행 전체가 비어있습니다"

**증상**:
```
헤더 검증 오류

원인: 첫 번째 행(헤더)이 완전히 비어있습니다.

해결 방법: 첫 번째 파일의 summary 시트 1행에 헤더 데이터를 입력해주세요.
```

**해결 방법**:
1. 첫 번째로 처리될 파일 확인
2. summary 시트의 1행에 헤더 입력
3. 예: `이름`, `나이`, `부서` 등

---

### 오류 3.2: "1행에 병합된 셀이 포함되어 있습니다"

**증상**:
```
헤더 검증 오류

원인: 첫 번째 행(헤더)에 병합된 셀이 포함되어 있습니다.

해결 방법: summary 시트 1행의 병합된 셀을 모두 해제해주세요.
```

**해결 방법**:
1. 엑셀에서 해당 파일 열기
2. summary 시트 1행 선택
3. "홈" → "병합하고 가운데 맞춤" → "셀 병합 취소"

**확인 방법**:
- 1행의 각 열이 독립적인 셀이어야 함
- 병합 아이콘이 활성화되어 있으면 병합된 상태

---

### 오류 3.3: "1행에 날짜 형식이 포함되어 있습니다"

**증상**:
```
헤더 검증 오류

원인: 첫 번째 행(헤더)에 날짜 형식이 포함되어 있습니다.

해결 방법: summary 시트 1행의 날짜 형식을 텍스트로 변경해주세요.
```

**해결 방법**:
1. 1행의 날짜 셀 선택
2. 우클릭 → "셀 서식"
3. "범주" → "텍스트" 선택
4. 확인

**예시**:
```
❌ 잘못됨: 2025-01-01 (날짜 형식)
✅ 올바름: "2025-01-01" (텍스트)
✅ 올바름: "날짜" (텍스트)
```

---

### 오류 3.4: "1행에 수식이 포함되어 있습니다"

**증상**:
```
헤더 검증 오류

원인: 첫 번째 행(헤더)에 수식이 포함되어 있습니다.

해결 방법: summary 시트 1행의 수식을 값으로 변환해주세요.
```

**해결 방법**:
1. 수식이 있는 셀 선택
2. 복사 (Ctrl+C)
3. 우클릭 → "선택하여 붙여넣기" → "값" 선택
4. 확인

**예시**:
```
❌ 잘못됨: =SUM(A2:A10), =CONCATENATE("이름", "1")
✅ 올바름: "합계", "이름1" (값으로 변환)
```

---

## 4. 실행 오류

### 오류 4.1: "모듈을 찾을 수 없습니다" (ImportError)

**증상**:
- 프로그램이 시작되지 않고 오류 메시지 표시

**해결 방법**:
1. 파일 재다운로드
2. 다른 위치에 저장하여 실행
3. 바이러스 백신 예외 추가

---

### 오류 4.2: 프로그램이 갑자기 종료됨

**증상**:
- 파일 선택 중 또는 병합 중 갑자기 종료

**원인**:
- 메모리 부족
- 손상된 엑셀 파일
- 디스크 공간 부족

**해결 방법**:
1. 다른 프로그램 종료 (메모리 확보)
2. 디스크 공간 확인
3. 파일 개수 줄이기
4. 손상된 파일 제외

---

## 5. 성능 문제

### 문제 5.1: 병합이 너무 느림

**증상**:
- 100개 파일 병합에 5초 이상 소요

**원인**:
- 파일 크기가 매우 큼
- 백신 실시간 검사
- 네트워크 드라이브 사용

**해결 방법**:
1. 파일을 로컬 드라이브로 복사
2. 백신 실시간 검사 일시 중지
3. 파일 개수 줄이기
4. 대용량 파일 확인 및 정리

**성능 기준**:
- 10개 파일: 1초 이내
- 50개 파일: 2-3초
- 100개 파일: 5초 이내

---

### 문제 5.2: 메모리 부족 오류

**증상**:
```
MemoryError: Unable to allocate memory
```

**해결 방법**:
1. 파일 개수 줄이기 (50개씩 나누어 병합)
2. 다른 프로그램 종료
3. 컴퓨터 재시작
4. RAM 업그레이드 고려

---

## 6. 바이러스 백신 문제

### 문제 6.1: Windows Defender가 실행을 차단

**증상**:
- "Windows의 PC 보호" 메시지
- "알 수 없는 게시자" 경고

**해결 방법**:

#### 옵션 A: 추가 정보로 실행
1. "추가 정보" 클릭
2. "실행" 버튼 클릭

#### 옵션 B: 예외 추가
1. Windows 설정 → 업데이트 및 보안
2. Windows 보안 → 바이러스 및 위협 방지
3. 설정 관리 → 제외 추가
4. ExcelMerger.exe 선택

상세 내용: ANTIVIRUS_CHECK.md

---

### 문제 6.2: 다른 백신이 삭제함

**증상**:
- 다운로드 후 파일이 사라짐
- 격리 폴더로 이동됨

**해결 방법**:
1. 백신 프로그램 열기
2. 격리 영역 확인
3. ExcelMerger.exe 복원
4. 예외 목록에 추가

---

## 7. 기타 문제

### 문제 7.1: 한국어가 깨짐

**증상**:
- 한국어 파일명이나 데이터가 이상하게 표시됨

**원인**:
- 시스템 로케일 문제 (드물음)

**해결 방법**:
1. Windows 설정 → 시간 및 언어
2. 지역 → 관리 → 시스템 로케일 변경
3. "한국어(대한민국)" 선택
4. 재시작

---

### 문제 7.2: 결과 파일을 열 수 없음

**증상**:
- 생성된 .xlsx 파일이 열리지 않음
- "파일이 손상되었습니다" 오류

**원인**:
- 디스크 공간 부족으로 불완전 저장
- 병합 중 프로그램 강제 종료

**해결 방법**:
1. 디스크 공간 확인
2. 프로그램 재실행
3. 다른 저장 위치 선택

---

## 8. 로그 파일 활용

### 로그 파일 위치
```
결과 파일과 같은 폴더
merger_log_yyyyMMdd_HHmm.txt
```

### 로그에서 확인할 사항
1. **SKIP된 파일**: 어떤 파일이 건너뛰어졌는지
2. **ERROR**: 어떤 오류가 발생했는지
3. **병합된 파일 수**: 예상과 일치하는지

### 로그 분석 예시
```
[2025-12-13 14:30:00] [INFO] 병합 시작
[2025-12-13 14:30:01] [SKIP] file1.xlsx - summary 시트가 없습니다.
[2025-12-13 14:30:02] [SUCCESS] file2.xlsx - 5 columns extracted
[2025-12-13 14:30:03] [ERROR] file3.xlsx - 파일이 잠겨있습니다...
```

**분석**:
- file1.xlsx: summary 시트 추가 필요
- file2.xlsx: 정상 처리
- file3.xlsx: Excel에서 닫아야 함

---

## 9. 도움 요청 전 체크리스트

문제가 해결되지 않으면 다음을 먼저 확인하세요:

- [ ] Windows 10 이상인가요?
- [ ] 엑셀 파일에 summary 시트가 있나요?
- [ ] 1행(헤더)이 올바르게 작성되었나요?
- [ ] 파일이 다른 프로그램에서 열려있지 않나요?
- [ ] 로그 파일을 확인했나요?
- [ ] 바이러스 백신이 차단하지 않나요?
- [ ] 디스크 공간이 충분한가요?
- [ ] 파일 개수가 100개 이하인가요?

---

## 10. 추가 지원

### 관련 문서
- **사용자 가이드**: USER_GUIDE.md
- **설치 가이드**: INSTALLATION.md
- **바이러스 검사**: ANTIVIRUS_CHECK.md

### 버그 리포트
- GitHub Issues에 보고
- 로그 파일 첨부
- 오류 스크린샷 포함

---

## 요약

대부분의 문제는 다음으로 해결됩니다:

1. **summary 시트 확인**: 모든 파일에 있는지 확인
2. **파일 닫기**: Excel 등에서 파일을 닫기
3. **헤더 확인**: 1행에 병합 셀, 날짜, 수식 제거
4. **바이러스 백신**: 예외 추가
5. **로그 확인**: 상세한 오류 정보 확인

여전히 문제가 있으면 로그 파일과 함께 버그 리포트를 작성하세요.
